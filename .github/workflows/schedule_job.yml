name: scheduledCron
on:
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '30 10 * * 6'

jobs:
  scheduled-task:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Executar tarefa agendada
        run: echo "Tarefa agendada executada em $(date)"
